
<h1>Nav Bar </h1>

<div class="row">
    <div class="col-1">
      <h2>Timer</h2>
    </div>

    <div class="col-5">
        <h2>List</h2>
        <ul>
            {{#each table_Name(WorkLinks)}}

                    <li>
                        <span class="border border-primary bg-light">{{id}}. {{name}}</span>
                        <button type="button" class="btn btn-primary" data-id="{{id}}">WorkLinks_1</button>
                    </li>
                    <li>
                        <span class="border border-primary bg-light">{{id}}. {{name}}</span>
                        <button type="button" class="btn btn-primary" data-id="{{id}}">WorkLinks_2</button>
                    </li>
                    <li>
                        <span class="border border-primary bg-light">{{id}}. {{name}}</span>
                        <button type="button" class="btn btn-primary" data-id="{{id}}">WorkLinks_3</button>
                    </li>
                
            {{!-- /each ends the "foreach" loop --}}
            {{/each}}
        </ul>
    </div>

    <div class="col-1">
        {{!-- Space in the middle --}}
    </div>

    <div class="col-5">
       <h2>Music Coctrol</h2>
        </ul>
    </div>

</div>